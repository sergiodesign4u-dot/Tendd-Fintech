READ THIS FIRST. This run reports ZERO boxes moved at both widths, and that is
the whole point of it: the founder asked for the trend line to become the brand
colour with a little gradient, and a colour change must move nothing. It moved
nothing, on all 55 coloured pages.

Run 2026-08-18 over D-Plot: petrol's fourth job. tokens.css gains --plot-line and
re-points --plot-area at the petrol tint; chart-placeholder.css reads the role
four times and fades the wash with a CSS mask behind @supports. The stroke went
from 2 to 2.5, which is a paint weight and not a box.

WHAT TO CHECK: 0 and 0. Colour is verified by computed style instead - stroke,
dot and readout all #1c6a76 in the light theme and #6bb0ba in the dark, with the
cursor's rule still on the neutral hairline.

FINGERPRINT DIFF, 55 pages at 360 and at 1440
  pages differing at 360:  0   (the promise is zero)
  pages differing at 1440: 0
  boxes differing at 360:  0
  boxes differing at 1440: 0

