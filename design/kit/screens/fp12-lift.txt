READ THIS FIRST. Zero boxes moved at either width, on all 55 coloured pages, and
that is the whole point of this run: stage 13's lift work touches comments, a
package file, a README section and the line every instrument uses to find
playwright. None of it is allowed to move a pixel, and none of it did.

Run 2026-08-18, the lift test and the run surface. What actually changed:
design/system/CLAUDE.md gains "What this folder does NOT carry" (the typeface, the
one dependency the folder cannot provide), base.css gains the note at the line a
person greps for, package.json arrives so `npm install` gets playwright, and seven
instruments resolve it local-first instead of from a global root - one of them,
fp.cjs, from an absolute path inside one machine's npx cache.

FINGERPRINT DIFF, 55 pages at 360 and at 1440
  pages differing at 360:  0   (the promise is zero)
  pages differing at 1440: 0
  boxes differing at 360:  0
  boxes differing at 1440: 0

