STAGE 09, STEP 1: the three patterns extracted, eight coloured pages moved onto them.
page-viewports compared : 56  (element count identical on every one)
elements compared       : 3998
elements with a change  : 137

INTERRUPTION: 2 shapes, 24 occurrences, on 12 page-viewports
    12x  marB: 24px -> 0px
        the announcement gives its bottom margin to the pattern
    12x  marT: 0px -> 24px
        the pattern declares the gap on the block that follows the announcement
   pages: add-subscription-error@1280, add-subscription-error@360, guided-reveal-empty@1280, guided-reveal-empty@360, history-trends-empty@1280, history-trends-empty@360, history-trends-error@1280, history-trends-error@360, subscription-detail-error@1280, subscription-detail-error@360, upgrade-payment-failed@1280, upgrade-payment-failed@360

ACT-FOOT: 2 shapes, 14 occurrences, on 12 page-viewports
    12x  marB: 16px -> 0px
        the action row gives its bottom margin to the pattern
     2x  marT: 0px -> 16px
        the pattern declares the gap on the line under the row
   pages: guided-reveal-empty@1280, guided-reveal-empty@360, history-trends-empty@1280, history-trends-empty@360, history-trends-error@1280, history-trends-error@360, history-trends@1280, history-trends@360, upgrade-payment-failed@1280, upgrade-payment-failed@360, upgrade@1280, upgrade@360

FOOT-16: 5 shapes, 5 occurrences, on 4 page-viewports
     1x  y: 592 -> 576
        the named decision: the foot gap is 16 at every width, so the closing line rises 16px at 1280
     1x  y: 262 -> 246
        the named decision: the foot gap is 16 at every width, so the closing line rises 16px at 1280
     1x  y: 485 -> 469
        the named decision: the foot gap is 16 at every width, so the closing line rises 16px at 1280
     1x  h: 851 -> 835
        the named decision: the foot gap is 16 at every width, so the closing line rises 16px at 1280
     1x  y: 867 -> 851
        the named decision: the foot gap is 16 at every width, so the closing line rises 16px at 1280
   pages: history-trends-empty@1280, history-trends-error@1280, upgrade-payment-failed@1280, upgrade@1280

FOOT-16-CHART: 29 shapes, 45 occurrences, on 1 page-viewports
     4x  y: 404 -> 420
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     3x  y: 446 -> 462
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     3x  y: 475 -> 491
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     2x  y: 307 -> 311
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     2x  y: 486 -> 502
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     2x  y: 483 -> 499
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     2x  y: 545 -> 561
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     2x  y: 542 -> 558
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     2x  y: 604 -> 620
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     2x  y: 601 -> 617
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     2x  y: 691 -> 707
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     2x  y: 734 -> 750
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  h: 115 -> 131
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  h: 113 -> 129
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  h: 63 -> 72
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  y: 339 -> 347
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  y: 370 -> 383
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  y: 312 -> 316
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  h: 56 -> 64
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  y: 507 -> 523
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  y: 492 -> 508
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  y: 533 -> 549
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  y: 534 -> 550
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  y: 566 -> 582
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  y: 551 -> 567
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  y: 592 -> 608
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  y: 593 -> 609
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  y: 625 -> 641
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
     1x  y: 610 -> 626
        the same 16px, on the one page that was already taller than its pane. The screen is a flex column at this width and the chart is the shrinkable item in it, so the 16px the foot gave back goes into the chart rather than off the bottom: the plot is 16px taller, everything under it sits 16px lower, and the page height does not change. Worth a line for stage 10: the chart is being squeezed here, before and after
   pages: history-trends@1280

ANIMATION: 3 shapes, 56 occurrences, on 3 page-viewports
    26x  opacity: 1 -> 0.999904
        the skeleton pulse, caught at a different frame. Three loading pages, no declared value involved
    18x  opacity: 1 -> 0.99988
        the skeleton pulse, caught at a different frame. Three loading pages, no declared value involved
    12x  opacity: 0.999889 -> 0.999862
        the skeleton pulse, caught at a different frame. Three loading pages, no declared value involved
   pages: add-subscription-loading@360, home-loading@360, subscription-detail-loading@360

UNEXPLAINED: 0 shapes, 0 occurrences, on 0 page-viewports

AT 360: no geometry moved at all. Every difference at the narrow viewport is a margin
changing owner, and the two arithmetics collapse to the same number there.
