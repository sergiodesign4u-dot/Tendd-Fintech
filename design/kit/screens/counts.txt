corpora: 55 grey pages, 55 coloured pages
components in the registry with a class: 71
css headers that disagree with the corpus:  0
component pages that disagree with the corpus: 0
headers whose count sentence this script will not parse: 0


the instrument cannot see these in the grey (the wireframes draw them under another name,
or the old selector needs a DOM). Their grey figure is left exactly as written:
  Checkbox              .check  was: .switch input
  Destination icon      .ic-*  was: -
  Muted line            .muted  was: .consequence, .context, .tone, .legal, .freshness, .removal, .pitch, p.notice, .p
  Select                .select  was: .field select
  Text input            .input  was: .field input
  Landing facts         .lp-facts  was: .lp-trust ul
